box: wercker/php
build:
  steps:    
    - script:
        name: install dependencies
        code: |-
            composer install --no-interaction
    - script:
        name: PHPSpec unit tests
        code: vendor/bin/phpspec run
    - script:
        name: PHPCS PSR2 checks
        code: vendor/bin/phpcs --standard=PSR2